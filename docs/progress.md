# 실험 진행 상황

전체 연구 설계는 [`experiment_context.md`](./experiment_context.md) 참고. 여기서는 진행 단계만 추적한다.

Deliverable Order (설계 문서 7절 기준) — 각 단계는 이전 단계에서 kriging이 해당 케이스에서 정상적으로 calibrate됨을 확인한 뒤에만 다음으로 넘어간다. 특정 케이스에서 kriging 자체가 calibrate되지 않으면 그건 결과가 아니라 케이스 설정 오류로 간주.

- [x] 1. Base case 재현 — 기존 single-model 결과를 새 코드베이스에서 재현
- [ ] 2. Variogram range 변화 실험 — 100~800m 8개 레벨 완료(레벨당 realization 1개), 추가 realization 예정
- [ ] 3. Nugget 변화 실험
- [ ] 4. Extrapolation / data configuration 변화 실험
- [ ] 5. Anisotropy 변화 실험
- [ ] 6. Sample count/sparsity 변화 실험 — 5%/2%/1% 3개 레벨 완료. 3개 레벨 모두 동일 10개 sample_seed(1001-1010)로 sample-placement replicate 완료(2026-09-18) — 단일-run 결과(맵/crossplot/calibration/hyperparameter 표)는 그대로 유지, MSE/UMG/variance_mean/length-scale은 10-replicate spread로 병행 제공
      (원래 "axis 5 TODO(미확정)"로 잡혀 있던 항목. 설계 문서 7절의 1~5번보다 먼저 착수 — 사용자 지시 2026-09-15)

## 보류(TODO) — 나중에 다시 판단할 것

- **sharpness 계열 지표(interval width, CRPS)를 workflow에서 일단 제외** (사용자 결정 2026-09-15).
  사유: 현재 보고 싶은 것을 제대로 표현하지 못한다는 판단. 대신 예측 분산의 합(`variance_sum`)/
  평균(`variance_mean`)을 비교 지표로 사용.
  - 적용 범위: **sample density 축만**. range axis(`results/processed/range_axis/`)는 width/CRPS를
    그대로 유지하며 이번 결정의 영향을 받지 않는다.
  - 코드는 살아있다: `src/evaluation.py`의 `gaussian_interval_widths`/`gaussian_crps`/
    `kriging_interval_widths`/`kriging_crps` 등과 그 테스트 9개는 전부 유지·검증된 상태이고,
    `evaluate_sample_density_axis.py`의 `EMIT_SHARPNESS_METRICS=False` 플래그만 True로 되돌리면
    즉시 복구된다.
  - 계산된 값도 버리지 않았다: `results/processed/sample_density_axis/metrics_parked_sharpness.csv`
    에 보존. 관련 진단(`crps_convergence.csv`, `kriging_backtransform_tail_sensitivity.csv`)도 유지.
  - 다시 볼 때 같이 고려할 것: (a) `docs/experiment_context.md` 5절이 interval width와 proper
    scoring rule을 평가 요구사항으로 명시하고 있으므로, 최종 논문에서 이들을 완전히 빼려면 그
    근거가 필요하다. (b) kriging의 width만 back-transform tail 가정에 민감하다는 측정 결과
    (1% 레벨에서 ±2σ/±4σ/±6σ에 따라 8.64/9.84/11.03으로 순위가 3가지로 바뀜)가 이 판단의
    배경 중 하나다.

## 로그
| 날짜 | 단계 | 상태 | 비고 |
|---|---|---|---|
| 2026-09-11 | - | 연구 handoff 문서 수령, 프로젝트에 반영 | 아직 구현 시작 전 |
| 2026-09-12 | 1 | Base case 파라미터 확정: grid 50x50 (20m cell, 1000x1000m 도메인), porosity mean=15.0/stdev=3.0, isotropic range≈300m, low nugget, 100 samples(10x10 규칙 격자, interior) | axis 5(sample count) TODO 추가 |
| 2026-09-13 | 1 | 샘플링 파라미터 변경: 이후 4개 방법 비교 실험(RBF+bootstrap, GP-MLE)에서는 100개(10x10 규칙 격자) 대신 500개(20%, 랜덤 interior sampling)로 변경됨 — 사용자 결정. `src/experiments/base_case_conditioning.py`의 `N_SAMPLES=500`이 단일 소스 | 위 100개 규칙 격자 기록은 base_case.py 자체(truth 생성)에는 영향 없음 — 참고용으로 유지 |
| 2026-09-13 | 1 | RBF+bootstrap 방법론 확정: (A) bootstrap replicate 수는 파이프라인 검증 단계인 지금은 `N_BOOTSTRAP=10` 유지, 논문용 최종 결과 산출 시 100~1000으로 상향 예정 — 아직 미확정이므로 그때 다시 결정. (B) bootstrap replicate마다 RBF 하이퍼파라미터(ε, smoothing)를 재튜닝하지 않고, 원본 454개 샘플로 한 번 CV-MSE 튜닝한 값을 10개 replicate 전부에 고정 재사용 — "bootstrap은 샘플셋 변동성만 포착한다"는 `experiment_context.md`의 Claim 1 서술과 부합하는 설계로 최종 확정(사용자 결정, 재론 불필요) | RBF+bootstrap/GP-MLE 코드는 이 결정대로 이미 구현되어 있음 (변경 없음) |
| 2026-09-14 | 1 | `numba` 미설치로 `geostatspy.geostats` import 자체가 실패하던 환경 문제 발견·해결 (`requirements.txt`에는 있었으나 `.venv`에 누락) | `results/raw`가 그동안 비어있던 이유로 추정 (RBF+bootstrap/GP-MLE도 truth 생성 경로에서 간접적으로 geostats.sgsim을 타므로 동일하게 영향받음) |
| 2026-09-14 | 1 | Simple kriging(`src/experiments/kriging.py`), SGS(`src/experiments/sgs.py`) 구현 완료 — base case 4개 방법(kriging/SGS/RBF+bootstrap/GP-MLE) 모두 구현 완료. kriging: NS(normal-score) 공간에서 `ktype=0`으로 `kb2d` 수행, point estimate는 `backtr_value`로 물리 단위 역변환, variance는 NS 단위 유지(비선형 역변환 미실시, 명시적으로 라벨링). SGS: 조건 데이터 좌표가 grid node와 정확히 겹쳐 `sgsim` 내부에서 특이행렬 에러가 나는 문제를 발견 → `sgsim` 호출용 DataFrame 사본에만 국소 jitter(1e-2m, `SGS_JITTER_SEED`) 적용 + post-hoc honor reinforcement(사후 원본 값 강제 재할당)로 해결, 사용자 승인됨(재론 불필요). `N_REALIZATIONS`는 `rbf_bootstrap.N_BOOTSTRAP`(=10) 재사용 | reviewer 검수에서 kriging.py의 `tmin/tmax`(kb2d 조건 데이터 필터) 버그(엉뚱한 상수 재사용으로 샘플 1개 침묵 드롭) 발견 → 즉시 수정·재검증 완료(454/454 전부 조건화 확인, 향후 재발 시 `RuntimeError`로 감지되도록 가드 추가) |
| 2026-09-14 | 1 | 샘플링 비율 재변경: 문헌조사(`docs/references.md`) 결과를 반영해 500개(20%) → **125개(5%, 2500-cell 중)**로 축소, 사용자 결정. `N_SAMPLES=125`가 단일 소스. 4개 방법(kriging/SGS/RBF+bootstrap/GP-MLE) 전부 재실행 완료(dedup 후 실사용 121개, 4개 방법 일치 확인) | 이전 500-샘플 run들은 `results/raw/`에 원본으로 보존, 수정 안 함 |
| 2026-09-14 | 1 | 평가 모듈 신규 구현: `src/evaluation.py`(MSE, UMG — 참고 코드베이스 `calc_UMG.py`/`dashboard.py` 로직 이식, kriging은 quantile back-transform 방식으로 NS↔물리 단위 불일치 없이 정확히 처리) + `src/experiments/evaluate_base_case.py`(4개 방법 tidy table `results/processed/base_case/metrics.csv` 생성, 조건샘플 cell은 4개 방법 공통으로 평가에서 제외) | **1차 결과(reviewer 검수 전, 잠정)**: MSE는 4개 방법이 3.31~3.73으로 비슷. UMG는 kriging 0.949, SGS 0.904, GP-MLE 0.961, RBF+bootstrap 0.528 — RBF+bootstrap의 심각한 과소평가(Claim 1과 부합)는 뚜렷하나, base case(낮은 nugget)에서는 GP-MLE가 kriging보다 UMG가 오히려 더 높게 나옴(Claim 2가 기대하는 패턴은 nugget이 커지는 axis 2/3에서 더 뚜렷해질 것으로 예상 — 해석은 reviewer 검수 및 사용자 확인 후 논의) |
| 2026-09-14 | 1 | reviewer 검수: 평가 모듈에 기능적 버그 없음 확인(참고 코드 수식과 한 글자 단위 대조, 마스크 정합성, 재현성 전부 확인). 판단 필요 사항으로 kriging UMG의 tail-clipping 비대칭 발견 — `p→1` 근처에서 kriging의 quantile back-transform 결과가 물리단위 고정 경계(`BACKTR_ZMIN/ZMAX`)로 수렴해, kriging variance가 큰 cell일수록 조기에 인위적으로 유리해지는 구조적 효과. 사용자 결정: `BACKTR_ZMIN/ZMAX`를 `POR_MEAN±4·STDEV`→`±10·STDEV`(=[-15,45])로 확장. 재실행 결과 kriging MSE·point estimate는 불변(bit-for-bit), UMG는 0.9491→0.9498로 미세 변화(예상대로 base case에선 영향 작음) — `p=1` 지점 자체는 어떤 폭이든 이 방법론의 수학적 특성상 항상 경계값에 수렴하므로 해소 대상 아님(코드 주석에 명시) | nugget/range 축(2, 3번)에서 kriging variance가 커지는 케이스가 늘어나면 이 효과가 더 커질 수 있으니 그때 다시 관찰 필요 |
| 2026-09-14 | 1 | `BACKTR_ZMIN/ZMAX`를 다시 `±10·STDEV`([-15,45]) → `±4·STDEV`([3,27])로 원복(사용자 결정). 사유: 같은 날 신규 추가한 kriging variance 물리단위 Monte Carlo 근사(`kriging_var_map_physical_mc.npy`)에서, 넓은 경계(zmax=45)가 정규점수 변환표 범위 밖 셀의 선형 tail 외삽을 45%까지 뻗게 만들어 해당 셀 물리단위 variance가 61 %²까지 치솟는(다른 곳은 대부분 5 이하) 역효과가 발견됨. `±4σ` 원복 시 위 UMG tail-clipping 문제가 재발할 수 있음을 감수. 새 run `results/raw/kriging/20260914T184654Z`: kriging point-estimate map(`kriging_mean_map_physical.npy`)은 이전 run(`20260914T144604Z`, ±10σ)과 bit-for-bit 동일(base case 셀들이 변환표 interior에 있어 tail bound 영향 없음) → MSE 불변(3.312265). UMG는 0.9498→0.9491로 미세 변화(±10σ→±4σ 원복 시에도 base case에서는 영향 미미, 예상과 부합). Monte Carlo 물리단위 variance 최댓값은 61.17 %² → 9.82 %²로 대폭 감소, `results/processed/base_case/make_truth_predictions_figures.py`의 shared variance 스케일(SGS/RBF/GP-MLE 기준, ~18.10)을 더는 초과하지 않아 kriging variance 패널이 더 이상 clip되지 않음(패널 주석을 clip 여부에 따라 조건부로 표시하도록 수정) | `metrics.csv`/`source_runs.json`/`accuracy_plot_curves.csv`/`accuracy_plot_comparison.png`/`crossplot_comparison.png`/5개 `qc_truth_predictions_*.png` 전부 재생성 완료. 커밋은 오케스트레이터가 검수 후 처리 |
| 2026-09-14 | 2 | Range axis 착수: range=50m(short)/800m(long) 확정(300m은 base case 기준선 재사용), realization은 1개(TRUTH_SEED=101)로 우선 진행(추가 예정). `build_vario()`/`get_base_case_truth()`/4개 방법 `main()`을 `hmaj1`/`hmin1`/`truth_seed` 인자로 파라미터화(기본값=base case 상수) — 무인자 호출 시 base case와 bit-identical 회귀 검증 통과(4개 방법 전부). 검색/튜닝 상수(ndmax, radius, `BACKTR_ZMIN/ZMAX`, `SGS_JITTER_MAGNITUDE`)는 range와 무관하게 base case 값 그대로 고정(one-factor-at-a-time) | 결과: MSE는 range가 짧을수록 4개 방법 모두 악화(예상대로). UMG는 RBF+bootstrap만 3개 range 전부 뚜렷이 낮음(0.166/0.528/0.461, Claim 1 일관되게 재현), GP-MLE는 0.96~0.995로 계속 잘 보정(이 axis에서는 Claim 2 미재현, 있는 그대로 기록). kriging UMG는 0.986→0.949→0.869로 range가 길어질수록 하락 — range=800에서 121개 샘플 중 상관 샘플(평균 108.6개)이 kriging `ndmax=50`/sgs `ndmax=20`보다 많아 모든 셀에서 탐색 이웃이 잘리는 것과 관련 가능성(사용자 결정: 지금은 그대로 두고 각주 처리, ndmax 확대 대조군은 보류) |
| 2026-09-14 | 2 | rbf_bootstrap.py 버그 수정: range=50에서 CV가 `best_smoothing=0.0`(정확 보간)을 선택했는데, bootstrap resample(복원추출)의 중복 (X,Y) 좌표가 RBF 보간행렬을 특이(singular)하게 만들어 `LinAlgError` 또는 NaN 맵 발생 → `best_smoothing==0.0`일 때만 중복 좌표를 사전 제거하도록 수정(하이퍼파라미터 튜닝 절차 자체는 변경 없음). reviewer 검증: smoothing=0 정확 보간에서는 중복 제거가 보간 함수값을 바꾸지 않음(수학적으로 무해, 특이행렬만 해소) — 사용자 결정: 현재 방식 유지, 방법 섹션에 각주만 추가 | RBF+bootstrap UMG가 range에 비단조(50m<300m>800m, "range가 길수록 ML에 유리" 가설과 다름) — 사용자 결정: 해석 보류하고 있는 그대로 보고, 원인 분석은 추가 axis 이후로 |
| 2026-09-14 | 2 | **축 재정의(사용자 결정)**: range=50m는 너무 짧아 제외, **100~800m를 100m 간격 8개 레벨**로 재설정. 300m(base case)/800m(직전 run)은 재사용하되 `samples.csv`의 X/Y/Por를 해당 range에서 재생성한 샘플과 대조해 검증 후 사용(reviewer 재현 확인, Por max\|diff\|=3.6e-15). 신규 run 24개(6레벨×4방법). 50m raw run은 보존하고 `results/processed/range_axis/excluded_runs.json`에 provenance 기록 | `results/processed/range_axis/metrics.csv` 160행(8레벨×4방법×5지표) |
| 2026-09-14 | 2 | **평가지표 확장(사용자 결정)**: 사용자 문제제기 "GP-MLE의 UMG가 높은데 구간만 넓혀 점수를 버는 것 아닌가"에 답하기 위해 **interval width와 CRPS**를 `src/evaluation.py`에 추가(기존 `mse`/`calc_umg` 등은 불변, git diff상 순수 추가만). 4개 방법 모두 **각자의 UMG가 쓰는 동일한 예측 분위수 표현**으로 환원해 계산(CRPS = 2∫₀¹ pinball_τ dτ, midpoint rule, n_tau=199). reviewer 독립 검증: 가우시안 closed-form과 brute-force 적분 대조(상대오차 ≤6e-15), properness(σ배율 스캔에서 1.0이 유일 최소), σ→0에서 MAE 수렴, 격자 수렴(199에서 상대오차 2.5e-5) 전부 통과 | **중요 정정**: 당초 "UMG는 구간을 넓히면 점수가 오른다"고 서술했으나 **틀린 주장**임이 reviewer 측정으로 확인됨(σ배율 0.5/1.0/2.0/3.0 → UMG 0.626/0.997/0.804/0.719로 σ=1에서 최대). UMG의 실제 한계는 (a) 과소산포 2배·과대산포 1배의 **비대칭 벌점**, (b) **집계 coverage 지표**라 sharpness를 구분 못 함. `src/evaluation.py` docstring과 `evaluate_range_axis.py` 주석의 잘못된 서술을 측정값과 함께 정정 |
| 2026-09-14 | 2 | 8레벨 결과: MSE는 range 증가에 따라 단조 감소 후 500m 부근에서 평탄(4개 방법 모두). **RBF+bootstrap은 전 레벨에서 UMG 0.32~0.53으로 압도적 최악이고 구간 폭도 kriging의 1/2~1/6, CRPS도 전 레벨 최악** — Claim 1이 range 전 구간에서 일관되게 재현됨(reviewer 확인: 가우시안/경험/fair 어느 CRPS 추정량으로도 결론 불변). **GP-MLE는 UMG 0.96~0.996로 전 레벨 최상위이나 CRPS에서는 8레벨 중 6레벨에서 kriging에 밀림** — 500m 이상에서 kriging/SGS 구간 폭은 계속 좁아지는데(1.70 @800m) GP-MLE만 ~2.05에서 평탄해진 뒤 반등(2.10 @800m, kriging 대비 1.24배). 즉 UMG 우위가 CRPS로는 이어지지 않음 | 사용자 결정 2건: (1) **ndmax 교란요인 현행 유지+각주** — range≥600m에서 kriging(ndmax=50)/SGS(ndmax=20)는 거의 모든 셀에서 상관 샘플(800m에서 평균 108.6/121개)을 잘라내는 반면 GP-MLE는 121개 전부 사용. kriging UMG의 장거리 하락(0.99→0.87)이 실제 현상인지 이 아티팩트인지 미분리 상태임을 논문에 각주로 명시. (2) **앙상블 CRPS 현행 유지** — SGS/RBF는 replicate 10개라 경험분위수 대신 가우시안 (mean,var) 모멘트 사용(각 방법 UMG 표현과 일치). 단 fair 추정량으로 바꾸면 800m에서 SGS(0.838) vs GP-MLE(0.844) 순위가 뒤집힘을 인지하고 유지 |
| 2026-09-14 | 2 | 문헌조사(`docs/references.md`에 CRPS 섹션 신규): **CRPS와 Deutsch goodness를 함께 쓴 선행연구를 찾지 못함**(두 계보가 reference list 상에서 서로 인용조차 안 함 — Semantic Scholar API로 직접 대조). 가장 근접한 선례는 Schmidinger & Heuvelink 2023(Geoderma 437, 116585; coverage 계열+CRPS 병용, 단 Deutsch G 미사용). **Szatmári & Pásztor 2019**(Geoderma 337, 1329–1340; UK/SGS/RFK/QRF를 accuracy plot+G로 비교)는 본 연구와 실험 구도가 가장 유사하므로 차별점 명시 필요 | ⚠️ 제출 전 해소 필요: Deutsch 1997 **페이지 범위**가 2차 출처마다 102–113 vs 115–125로 엇갈림, 원문(CCG 서버 스캔 PDF)에서 "goodness는 구간 확대로 부풀릴 수 있어 precision 병행 필요"라는 서술 존재 여부 **미검증**(스캔 이미지라 자동 확인 실패, 사람이 직접 열어봐야 함) |
| 2026-09-14 | 2 | reviewer가 지적한 명확한 문제 3건 수정: (1) `pytest`가 `.venv`·`requirements.txt` 양쪽에 없어 테스트 실행 안내가 재현 불가였던 문제 → pytest 8.3.5 설치 후 `requirements.txt` 갱신, 9개 테스트(신규 6+기존 3) 전부 통과 확인. (2) `sgs.py` QC 그림 제목이 range 파라미터화 후에도 "base-case range"로 고정돼 있던 stale 라벨 수정. (3) 위 UMG 관련 잘못된 docstring 2곳 정정 | `results/raw/`의 기존 QC png는 원본 보존 원칙상 그대로 두므로, 이미 생성된 run의 `qc_sgs.png`에는 stale 제목이 남아있음(이후 run부터 수정 반영) |
| 2026-09-15 | 5 | Sample density 축 신규(기존 "axis 5 TODO" 착수, 사용자 지시): **5%(n=125→실사용 121) / 2%(n=50) / 1%(n=25)**, range=300m 고정, 레벨당 realization 1개. `get_conditioning_samples`/`get_base_case_conditioning_data`와 4개 방법 `main()`에 `n_samples` 인자 추가(기본값=125 → 무인자 호출 시 base case와 bit-identical, reviewer가 14개 배열+5개 텍스트 파일 max abs diff 0.0으로 재현 확인). 5%는 base case run 재사용(검증 후), 신규 run 8개. 지표는 range 축과 동일한 5개(mse/umg/width_mean/width_p95/crps) | `results/processed/sample_density_axis/metrics.csv` 60행. 평가 셀 수가 레벨마다 다름(2379/2450/2475) — 세 레벨 공통 2312셀로 재계산 시 전 지표 변화 <1.5%, 순위 변화 없음(reviewer 확인) |
| 2026-09-15 | 5 | **샘플 추출 방식 정정**: 당초 nested subset으로 구현했다가 사용자 지시("전체 ground truth에서 뽑아라")로 레벨별 독립 호출로 되돌림. 그러나 reviewer가 **"independent"라는 기술이 사실과 다름**을 발견: `src/sampling.py`가 `xs = rng.uniform(...)` → `ys = rng.uniform(...)`를 같은 RandomState에 순차 호출하므로, 고정 seed에서 **n=25 draw의 X좌표는 n=125 draw의 첫 25개와 완전히 동일하고 Y만 다르다**(스트림 위치 25~49 vs 125~149). 즉 세 레벨은 nested도 independent도 아닌 **X 공유 / Y 상이** 구조. 셀 교집합(4/25, 4/50, 0/25)만으로는 이 결합이 드러나지 않음. `sample_density_axis.py` docstring, `sample_overlap.json` note, 모든 그림 캡션, `base_case_conditioning.py`의 섹션 헤더·docstring에서 "independent" 표현 제거하고 실제 구조로 정정. `sampling.py`의 `random_interior_samples` docstring에도 이 성질을 원천 기록 | 수치 결과는 전부 유효(순수 기술 문구 정정). 그림 축 라벨도 requested(5.0%, n=125) → actual(4.84%, n=121)로 수정 |
| 2026-09-15 | 5 | 결과(레벨별 4방법): 1%(n=25)에서 MSE kriging 6.851 / sgs 7.756 / rbf 7.672 / gp 7.872, UMG 0.974 / 0.910 / 0.422 / 0.975, width_mean 3.553 / 3.440 / 1.016 / 3.838, CRPS 1.430 / 1.568 / 1.877 / 1.558. **RBF+bootstrap의 width가 축 전체에서 거의 평평**(1.105 → 1.028 → 1.016)한 반면 나머지 셋은 약 1.5배 증가(2.36~2.42 → 3.44~3.84). GP-MLE의 학습된 length_scale은 116.13 / 120.09 / **54.09**m로 1%에서 붕괴(practical range √6·ℓ 환산 시 284 / 294 / **132**m, truth spherical range 300m), 학습된 noise variance는 1.601 / 1.627 / 0.492 (truth nugget 0.45 대비 3.6× / 3.6× / 1.09×) | reviewer가 480개 초기값(restart 8~200 + 3차원 격자)으로 재적합해 **세 레벨 모두 동일 해로 수렴** 확인 — 1%의 ℓ=54m는 optimizer 실패가 아니라 전역 MLE 해 |
| 2026-09-15 | 5 | reviewer가 발견한 교란요인·민감도 (해석 없이 수치만 기록): (1) **conditioning 샘플 평균 편향** — truth field 전체 평균 15.000 대비 샘플 평균 14.104(5%, z=−3.06) / 14.628(2%, z=−0.69) / **16.070(1%, z=+1.84)**, MSE에서 bias²가 차지하는 비중 0.2~0.7% / 0.2~3.8% / **13.7~16.3%**. SGS 1% realization 평균이 16.089±0.424로 흐르는 것도 이 샘플 편향(16.070)에서 파생. (2) **kriging의 back-transform tail 가정 민감도**(1%, 변환표 25점, CRPS 적분이 91.7% 셀에서 tail 외삽 통과) — CRPS는 ±2σ/±4σ/±6σ에서 1.4239/1.4297/1.4395로 GP(1.5576)·SGS(1.5682)보다 항상 낮아 **순위 견고**하나, width_p95는 8.643/9.838/11.034로 **순위가 3가지로 바뀜**(SGS 9.256, GP 10.328 사이를 오감). (3) **RBF의 1% smoothing flip**(0.1 → 0.0, dedup 10/10 발동) — CV paired t-test t=1.94 **p=0.124**(fold당 test 5점)로 노이즈 수준이며, dedup 없이는 `LinAlgError`로 실행 불가. sm=0.1 강제 시 UMG 0.3807 / w95 2.442 vs production 0.4221 / 2.734. (4) **SGS `ndmax=20` 구속 셀 비율**이 레벨마다 다름: 5% 68.5% / 2% 11.2% / 1% 0% | 사용자 지시로 해석은 보류, 수치만 기록. 진단 CSV 4종 신규: `conditioning_sample_bias.csv`, `mse_bias_variance_decomposition.csv`, `gp_hyperparameter_scale_conversion.csv`, `kriging_backtransform_tail_sensitivity.csv` |
| 2026-09-17 | 5 | **신규 그림 2개 추가**(사용자 지시): (a) `00_length_scale_vs_density.png` — kriging/SGS/RBF+bootstrap/GP-MLE 4개 방법이 밀도축 3레벨에서 실제로 쓴 "length/range"를 한 그래프에 비교. 수치: kriging·SGS는 3레벨 전부 입력 variogram range=300.0m로 상수(각 run manifest의 `params.variogram.hmaj1`에서 확인, 데이터로 피팅되는 값이 아님). RBF+bootstrap은 CV로 고른 gaussian 커널 shape parameter epsilon을 0.05-correlation-cutoff 거리로 환산해 표시(이 값은 kriging/GP처럼 데이터의 공간상관거리를 추정한 통계량이 아니라 보간 커널 형태 파라미터의 기하학적 환산치일 뿐임을 캡션에 명시). **[2026-09-17 정정 — 아래 EPSILON_GRID 재설계 항목 참조]** 이 항목을 작성할 당시 수치는 "3레벨 전부 epsilon=0.01 → 173.08m로 상수"였으나, 그것은 구 `EPSILON_GRID`(9점)에서 나온 값이고 같은 날 격자를 재설계한 뒤 재실행한 현재 값은 **274.25 / 274.25 / 219.92m**(5% / 2% / 1%)이다. 현재 `length_scale_by_method.csv`와 `00_length_scale_vs_density.png`는 후자 기준으로 갱신돼 있다. GP-MLE만 밀도에 반응: practical range 284.26 / 293.94 / **132.41**m(1%에서 급락, 기존 로그의 length_scale 116/120/54m와 동일 현상). (b) `00_experimental_variogram.png` — 밀도축이 공유하는 단일 truth realization(seed=101)의 experimental variogram을 exhaustive 계산(2500셀 전체 쌍 ~3.1M) 대신 200,000개 무작위 pair 서브샘플로 lag-bin(25m×30bin)해서 이론 spherical 모델(nugget=0.45, sill=9.0, range=300m, 물리단위로 affine 변환된 값)과 겹쳐 그림 | 신규 스크립트 `results/processed/sample_density_axis/make_length_and_variogram_figures.py`, CSV 2종(`length_scale_by_method.csv`, `experimental_variogram_truth.csv`). 두 그림 모두 파일명에 `00_` 접두사를 붙여 `results/figures/sample_density_axis/` 내 다른 그림보다 정렬상 앞에 오도록 함(이 프로젝트엔 별도 보고서 문서가 없어 figures 디렉토리가 사실상 보고서 역할). reviewer 검수에서 명확한 문제 2건 발견·수정: (1) 캡션의 range 값이 `HMAJ1` import 대신 리터럴 `300.0`으로 하드코딩돼 있던 것 수정, (2) truth 필드 empirical mean/variance 체크 관련 docstring이 "finite-sample deviation 예상"이라고 과장 서술했던 것을 "GSLIB.affine은 선형변환으로 항상 정확히 일치하며 공간구조의 독립적 검증이 아니다"로 정정. 수정 후 재실행해 수치·그림 불변 확인, `git status`로 의도한 5개 파일 외 변경 없음 확인 |
| 2026-09-17 | 5 | **experimental variogram의 우상향 현상 검증**: (b) 그림에서 lag>400m 구간의 경험적 semivariance가 이론 sill(9.0)을 넘어 ~10.9까지 계속 우상향하는 게 관찰됨. reviewer가 같은 truth 필드에 대해 **exhaustive 전수 계산**(3,123,750쌍 전체, 서브샘플링 없음)을 별도로 수행해 대조한 결과 거의 동일한 우상향 곡선이 나와, **코드 버그가 아니라 이 particular 단일 realization(seed=101)의 진짜 통계적 특성**임을 확인(원인: range=300m가 도메인 크기 1000m×1000m 대비 상당히 커서, 단일 unconditional realization이 우연히 큰 스케일의 "가짜 추세"를 가짐 — 선형 추세 제거 시 전체 분산 9.0 중 약 2.0(22%) 감소 확인). semivariance 공식·lag bin 집계·이론곡선 유도·RBF epsilon 변환식·kriging/SGS/RBF 3레벨 수치 전부 reviewer가 직접 재계산해 정확함을 확인 | **판단 필요 사항(사용자 확인 대기)**: (1) 이 우상향 패턴을 논문에 어떻게 서술할지 — 단순 한계로 캡션에만 명시할지, 여러 truth realization(seed 101-105)으로 "이 seed만의 특이 현상"임을 보여줄지, 아니면 이 특정 truth 필드에서 "range=300m"라는 가정이 근사적으로만 맞다는 점을 다른 결론(RBF/GPR misspecification 서술 등)에도 반영할지. (2) experimental variogram의 `MIN_PAIRS_PER_BIN=30` 하한선(관례적 선택, 이번 실행에서는 전 bin 345쌍 이상이라 실질 영향 없음)이 다른 축에 재사용될 때도 적절한지 |
| 2026-09-17 | 1, 2, 6 | **RBF `EPSILON_GRID` 재설계 + RBF run 전면 재실행**. 변경: 구 격자 9점(0.05-cutoff 길이로 1.7~1730.8m; 이 중 3점이 셀 크기 20m보다 짧고 1점이 도메인 대각선 1414m 초과) → 신 격자 25점 로그균등(10~2000m, 간격 24.7%). 범위 근거는 도메인 기하(셀 크기 20m, 대각선 1414m)이며 관측된 최적값에 맞춘 것이 아님. 재설계 사유(측정 사실): (1) density 3레벨이 전부 eps=0.01(173.1m)로 동결, (2) range 8레벨이 3개 값(86.5/173.1/576.9m)으로만 나뉘고 400·500·600·700·800m 다섯 레벨이 576.9m 하나로 뭉개짐, (3) 동일 CV 절차(같은 fold·`CV_SEED`·`SMOOTHING_GRID`)를 45점 fine grid로 재생하면 진짜 CV 최적이 268.5/268.5/215.6m(5%/2%/1%)이고 생산값의 CV-MSE가 각각 9.9%/15.2%/3.7% 열등. 결과: density 동결 해소(274.2/274.2/219.9m), range는 3개 값→4개 값(113.4/219.9/274.2/342.0m)으로 분리됐으나 400~800m 다섯 레벨은 여전히 342.0m 하나로 포화. range 축 RBF UMG는 이전의 300m 피크(0.321~0.528, argmax=300m)가 사라지고 200~800m가 폭 0.012의 밴드(0.394~0.401)로 평탄해짐(엄밀한 단조는 아님). density 축 RBF `variance_mean`은 0.765/0.661/0.758(밀도 무반응) → 0.352/0.867/1.336(단조증가). base case RBF: MSE 3.7256→3.7524(+0.72%), UMG 0.5279→0.3990(-24.4%) — CV-MSE는 8.98% 개선됐는데 전영역 MSE는 악화(range 축도 8레벨 중 7레벨에서 MSE 악화) | 재실행 범위: **RBF run 10개만 신규**(base case = range300 = density5%는 한 run을 공유), kriging/SGS/GP-MLE run·코드·pin은 전부 불변. 세 `source_runs.json`의 `rbf_bootstrap` 항목만 교체. 변경된 것은 `EPSILON_GRID` 하나뿐이며 `SMOOTHING_GRID`/`CV_FOLDS`/`CV_SEED`/`N_BOOTSTRAP`/`BOOTSTRAP_SEED`/`RBF_KERNEL`/dedup 로직은 그대로(one-factor-at-a-time). reviewer의 density 축 2×2 분해(구·신 epsilon × 구·신 smoothing): 5% 레벨 `variance_mean` 급락은 100% smoothing 0.1→1.0 스냅 때문이고(신 epsilon+구 smoothing이면 오히려 0.946으로 상승), 2%·1% 레벨의 상승은 전적으로 epsilon 때문 — 한 축 위에서 레벨마다 원인이 다름. reviewer 재현성 검증 통과: base case pin 준수(4개 방법 12자리 일치), RBF run 재현성(하이퍼파라미터 bit-exact), 시드 기록, `pytest tests -q` 9 passed. **판단 필요 사항(사용자 확인 대기)**: `SMOOTHING_GRID`(10배 간격)가 CV 최적을 표현하지 못함 — 10레벨 중 9레벨의 진짜 최적이 0.159~0.631 구간인데 0.1 또는 1.0으로 스냅됨(1.6~2.5배 이탈, 상한 10.0은 문제 아님). 촘촘한 smoothing 격자로 풀면 range100/200/400의 선택 epsilon까지 바뀌어 range400이 342.0m→274.2m로 분리됨. 현재 발표 수치는 제대로 해상된 CV 대비 RBF의 variance를 13~55% 낮게, MSE를 전 레벨 열등하게 보이게 함 |
| 2026-09-17 | 1, 6 | **reviewer 지적 "명확한 문제" 수정 + stale 산출물 갱신**. (1) 재현성 버그: `evaluate_base_case.py`만 `source_runs.json`을 출력으로만 쓰고 입력은 `latest_run_dir()`(최신 타임스탬프)로 잡고 있었음 — 다른 모든 평가/그림 스크립트는 이 파일을 입력으로 읽음. 이번 재실행에서 base case RBF run이 최신이 된 것은 병렬 실행된 range 축 run 9개보다 15초 늦게 시작된 우연이었고, 순서가 달랐다면 range=800m run이 base case로 들어갔을 것. 기존 drift 체크는 `samples.csv`의 X/Y만 비교했는데 range 축 레벨들은 `sample_seed`·`n_samples`가 같아 X/Y가 완전히 동일(`Por`만 다름)하므로 이를 잡지 못함. 수정: `source_runs.json`이 있으면 그것을 pin으로 읽고(없을 때만 latest로 폴백하며 폴백 사실을 stdout에 출력), drift 체크가 X/Y에 더해 `Por`까지 비교하고 불일치 시 `RuntimeError`로 중단. (2) `rbf_bootstrap.py`의 stale 주석 2곳 수정: dedup 분기 설명의 "base case는 best_smoothing=0.1이고 bit-for-bit 회귀검사로 확인" → 현재 base case는 1.0이고 그 회귀검사는 같은 파일이 RBF에 대해 더 이상 성립하지 않는다고 선언한 검사이므로, 근거를 "pin된 base case run의 manifest가 `best_smoothing=1.0`·`n_bootstrap_deduplicated=0`"으로 교체; bootstrap 주석의 "500 sample locations" → `N_SAMPLES` 125(실사용 121) | 갱신한 stale 산출물: `range_axis/qc_summary.csv`(rbf epsilon 0.02/0.01/0.003 → 0.015262/0.007870/0.006311/0.005061), `sample_density_axis/qc_summary.csv`(0.01 고정 → 0.006311/0.006311/0.007870), `sample_density_axis/mse_bias_variance_decomposition.csv`(RBF 3행), `crps_convergence.csv`(RBF 8행), `metrics_parked_sharpness.csv`(RBF 9행; `EMIT_SHARPNESS_METRICS`를 일시적으로 True로 두고 재실행해 값을 옮긴 뒤 **False로 복구**, 파킹 결정 자체는 유지 — 나머지 27행은 불변 확인), `kriging_backtransform_tail_sensitivity.csv`(parked CSV에서 읽어오는 `rbf_bootstrap_*` 열: interval_width_p95 2.973257→2.132410, crps 1.289521→1.327584 등), `results/figures/base_case/qc_rbf_bootstrap.png`(구 run `20260914T135501Z`의 파일과 md5가 같고 제목에 `eps=0.01, sm=0.1`이 박혀 있던 것을 pin된 새 run `20260917T230913430600Z`의 QC 그림으로 교체). 수정 후 `evaluate_base_case.py`를 저장소 명령만으로 재실행해 `metrics.csv`/`accuracy_plot_curves.csv`/`source_runs.json`이 바이트 단위로 동일하게 재생성됨을 확인(직전 작업은 저장소 밖 임시 스크립트로 돌린 상태였음). **재생성 못한 것 1건**: `results/figures/sample_density_axis/interval_width_p95_vs_sample_density.png` — 2026-09-15 파킹 때 이 그림을 그리는 코드가 `make_sample_density_figures.py`에서 제거됐고(커밋 이력상 코드가 남아 있는 버전이 없음, 캡션 상수만 보존) 재생성하려면 새로 그리기 코드를 작성해야 하므로 손대지 않고 그대로 둠 — 소스인 `metrics_parked_sharpness.csv`는 갱신됐으나 이 그림은 여전히 구 수치임(같은 파일의 `TAIL_ASSUMPTION_CAPTION` 안 "RBF+bootstrap, 2.73" 수치도 같은 이유로 stale) |
| 2026-09-18 | 6 | **Sample-placement replicate 축을 5%(n=125)에서 2%(n=50)/1%(n=25)로 확장** (사용자 지시, 사전 확인 질문으로 범위 확정: "2%/1%도 10개씩 새로 실행"). 동일 10개 seed(1001-1010)를 세 레벨 모두에 재사용 — 5%의 기존 40개 run은 재사용(재실행 안 함, `source_runs_level5_n125.json`에 원본 스냅샷 보존), 2%/1%는 80개 신규 run. 120개 (level,replicate,method) 셀 전부 samples.csv atol=1e-10 cross-check로 검증(재사용분 포함). `metrics_vs_sample_density.png`(MSE/UMG/variance_mean)와 `00_length_scale_vs_density.png`(GP-MLE/RBF+bootstrap만; kriging/SGS는 고정 입력이라 spread 없음)를 10-replicate 지터+mean±1std로 갱신 | **결과**: RBF+bootstrap은 밀도가 희박해질수록 UMG 평균이 낮아지는 동시에(0.551→0.549→0.440) 10개 replicate 간 변동폭도 커짐(std 0.069→0.093→0.144) — 나머지 3개 방법은 전 레벨에서 std ≤0.08로 안정. **같은 10-seed를 레벨 간 재사용했을 때의 X/Y 결합** 실측(30개 replicate×level-pair 전체): raw X 동일 30/30, raw Y 동일 0/30, distinct-X 부분집합 30/30 — 기존 단일-seed 축과 동일 패턴. 단 distinct-Y 부분집합은 기존 축이 "항상 거짓"이라 서술한 것과 달리 6/30에서 우연히 성립(작은 n의 grid snapping/dedup 우연, 설계된 성질 아님) — `sample_seed_level_overlap.json`에 기록, 캡션에 오단순화 없이 명시. **판단 필요 사항(사용자 확인 대기, 아직 미해결)**: (1) RBF의 length-scale이 이산 CV epsilon grid에서 선택되어 여러 replicate가 동일 grid point로 수렴(예: 219.92m·274.25m가 반복) — 이 실험 목적(replicate 간 미세한 차이 측정)에 이 grid 해상도가 충분한지는 미확정, grid 자체는 이번 작업에서 변경 안 함. (2) 스키마 변경으로 구 `make_sample_replicate_figures.py`가 깨질 것을 coder가 발견해 스코프 밖에서 3-레벨 PNG로 자체 재작성(레벨별 `metrics_replicate_spread_level{5,2,1}.png`로 분리, 구 파일 삭제) — reviewer가 필요성과 무결성(dangling reference 없음)을 확인했으나, 사전 승인 없는 스코프 확장을 매번 사후 보고로 허용할지는 별도 판단 사항 | crossplot/calibration/maps/hyperparameter 표는 단일-run 그대로 유지(변경 없음). 관련 파일: `results/processed/sample_replicate_axis/{metrics,metrics_summary,length_scale_by_replicate,sample_seed_level_overlap,source_runs,source_runs_level5_n125,replicate_seeds}.*` |
| 2026-09-18 | 6 | **Length-scale case study (사용자 지시)**: 1% 레벨(n=25)에서 GP-MLE/RBF+bootstrap의 length-scale이 최소/최대인 replicate를 찾아(GP: rep0=144.6m min / rep8=739.9m max; RBF: rep1=141.4m min / rep5=1603.8m max, `length_scale_by_replicate.csv`에서 계산, 하드코딩 아님) truth+samples/realization/prediction/variance/UMG-plot 5패널 케이스 스터디 2장(방법별 min/max 2행) 생성. UMG는 그림 스크립트 안에서 재계산 후 pinned `metrics.csv` 값과 대조(4건 전부 정확히 일치: gp rep0=0.8461/rep8=0.7869, rbf rep1=0.2781/rep5=0.3731) — 불일치 시 raise. 포로시티 스케일은 두 행 공유, variance 스케일은 행별 독립(각자 최대값 주석). **관찰**: RBF+bootstrap은 length-scale이 11배 차이나는 두 극단에서도 UMG가 0.278→0.373으로 소폭만 변함 — 같은 레벨의 kriging 최악 replicate(UMG 0.816)보다도 훨씬 낮아, RBF의 under-coverage는 특정 epsilon 선택의 부작용이 아니라 구조적 현상임을 시사. 같은 지시로 "calibration curve" 명칭을 **"UMG plot"으로 전면 변경**(디스플레이 텍스트만; 함수명/파일명/`accuracy_plot_curves.csv`는 불변) — `make_sample_density_figures.py`의 `plt.suptitle` 1줄만 수정, 나머지 15개 그림은 재실행 후 byte-identical 확인 | 신규 스크립트 `results/processed/sample_replicate_axis/make_length_case_study_figures.py`. reviewer가 `safe_sqrt_variance`를 `evaluate_sample_replicate_axis.py`에서 import한 선택(로컬 복제 대신)을 판단 필요 사항으로 플래그 — 이 저장소에 "import-time side-effect만 피하면 됨"(좁은 근거)과 "스크립트는 독립 실행 가능해야 함"(넓은 근거)이 공존하며, 코더는 좁은 해석을 택함. 버그는 아니고 스타일 정책 결정 사항으로 남김 |
| 2026-09-18 | 6 | **Length-scale case study에 6번째 열(accuracy crossplot) 추가**(사용자 지시). truth vs. point estimate 산점도 + MSE 재계산·pinned metrics.csv 대조(4건 모두 일치: gp rep0=7.9155/rep8=7.5143, rbf rep1=8.1102/rep5=7.5674). RBF+bootstrap의 6번째 열은 `point_estimate_map.npy`(단일 fit)를 쓰고 기존 3번째 열(prediction mean)의 `bootstrap_mean_map.npy`(앙상블 평균)와는 의도적으로 다른 배열 — 두 열이 실제로 다른 소스임을 reviewer가 코드 대조로 확인. GP-MLE는 두 열 다 `posterior_mean_map.npy` 사용(구분 없음, 확인됨) | 변경 파일은 `make_length_case_study_figures.py` 1개 + PNG 2개뿐, 다른 스크립트/raw 데이터/metrics.csv는 불변 |
| 2026-09-18 | 6 | **Length scale vs. smoothing parameter 산점도 신규**(사용자 지시): GP-MLE/RBF+bootstrap 각각 1행 3열(5%/2%/1%) scatter, x=length scale(기존 `length_scale_by_replicate.csv` 재사용, 재계산 안 함), y=RBF는 `params.best_smoothing`(문자 그대로 smoothing), GP-MLE는 대응 개념이 없어 `params.fitted_hyperparameters.noise_variance_real_units`(GP의 nugget/정규화 항)를 "smoothing-equivalent"로 명시적으로 라벨링해 사용(오케스트레이터 결정, 그림 y축 텍스트에 직접 명시해 리터럴 smoothing과 혼동되지 않게 함). reviewer가 두 축·조인 로직·GP 라벨링 문구(렌더링된 이미지에서 픽셀 텍스트까지 직접 확인)를 검증 — 명확한 문제 1건 발견: RBF 그림에서 CV grid 이산성 때문에 동일 좌표에 찍힌 replicate들의 번호 주석이 완전히 겹쳐 판독 불가(데이터 자체는 진짜 동점, 버그 아님). 즉시 수정: 동일 좌표 그룹을 log-space tie-key로 묶어 "0,1,8" 형태의 결합 라벨 하나로 표시 → 재검증 후 반영 | 신규 스크립트 `results/processed/sample_replicate_axis/make_length_vs_smoothing_figures.py`. RBF 5% 레벨에서 3개 tie group 확인((176.36,0.1)×2, (219.92,0.1)×3, (274.25,0.1)×3), GP-MLE는 전 레벨에서 tie 없음(noise variance가 연속적으로 피팅되기 때문, 확인됨) |
